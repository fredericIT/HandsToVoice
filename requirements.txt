# HandsToVoice — Dependencies
# Install: pip install -r requirements.txt

# Computer Vision
opencv-python>=4.8.0
mediapipe>=0.10.0

# Machine Learning
tensorflow>=2.14.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# Text-to-Speech
gTTS>=2.4.0
pyttsx3>=2.90

# Audio Playback
pygame>=2.5.0

# GUI
PyQt5>=5.15.0

# Voice listening (hearing person's speech -> sign video)
SpeechRecognition>=3.17.0
transformers>=5.17.0
# CPU-only PyTorch is enough and ~10x smaller than the default CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.14.0

# Utilities
Pillow>=10.0.0
matplotlib>=3.10.0   # evaluate_model.py confusion-matrix image
